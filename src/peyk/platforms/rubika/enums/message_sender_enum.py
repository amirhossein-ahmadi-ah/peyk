from __future__ import annotations

class MessageSenderEnum:
    """Provide the documented Rubika constant values for ``MessageSenderEnum``.

    The class preserves the existing public fields and wire-format behavior.
    """
    USER = "User"
    BOT = "Bot"


from __future__ import annotations

class ForwardedFromEnum:
    """Provide the documented Rubika constant values for ``ForwardedFromEnum``.

    The class preserves the existing public fields and wire-format behavior.
    """
    USER = "User"
    CHANNEL = "Channel"
    BOT = "Bot"


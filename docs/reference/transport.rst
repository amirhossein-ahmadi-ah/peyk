Transport API
=============

The transport layer supplies the asynchronous HTTP session, multipart support,
retry handling, logging hooks, and transport-level errors shared by Peyk's
platform clients.

.. autosummary::
   :toctree: generated/transport
   :recursive:

   peyk.transport

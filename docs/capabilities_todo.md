# Capability audit TODO

UNKNOWN or low-confidence items from the audited registry. Each item has a concrete live-token probe suggestion.

- **telegram / `keyboards.rubika.selection`** — Not independently established by the Phase 1 audit. Probe: issue the smallest platform API request that directly exercises this feature with a live bot token and record the response schema/error.
- **telegram / `keyboards.rubika.calendar`** — Not independently established by the Phase 1 audit. Probe: issue the smallest platform API request that directly exercises this feature with a live bot token and record the response schema/error.
- **telegram / `keyboards.rubika.number_picker`** — Not independently established by the Phase 1 audit. Probe: issue the smallest platform API request that directly exercises this feature with a live bot token and record the response schema/error.
- **telegram / `keyboards.rubika.string_picker`** — Not independently established by the Phase 1 audit. Probe: issue the smallest platform API request that directly exercises this feature with a live bot token and record the response schema/error.
- **telegram / `keyboards.rubika.location`** — Not independently established by the Phase 1 audit. Probe: issue the smallest platform API request that directly exercises this feature with a live bot token and record the response schema/error.
- **telegram / `keyboards.rubika.camera_image`** — Not independently established by the Phase 1 audit. Probe: issue the smallest platform API request that directly exercises this feature with a live bot token and record the response schema/error.
- **telegram / `keyboards.rubika.camera_video`** — Not independently established by the Phase 1 audit. Probe: issue the smallest platform API request that directly exercises this feature with a live bot token and record the response schema/error.
- **telegram / `keyboards.rubika.gallery_image`** — Not independently established by the Phase 1 audit. Probe: issue the smallest platform API request that directly exercises this feature with a live bot token and record the response schema/error.
- **telegram / `keyboards.rubika.gallery_video`** — Not independently established by the Phase 1 audit. Probe: issue the smallest platform API request that directly exercises this feature with a live bot token and record the response schema/error.
- **telegram / `keyboards.rubika.file`** — Not independently established by the Phase 1 audit. Probe: issue the smallest platform API request that directly exercises this feature with a live bot token and record the response schema/error.
- **telegram / `keyboards.rubika.audio`** — Not independently established by the Phase 1 audit. Probe: issue the smallest platform API request that directly exercises this feature with a live bot token and record the response schema/error.
- **telegram / `keyboards.rubika.record_audio`** — Not independently established by the Phase 1 audit. Probe: issue the smallest platform API request that directly exercises this feature with a live bot token and record the response schema/error.
- **telegram / `keyboards.rubika.textbox`** — Not independently established by the Phase 1 audit. Probe: issue the smallest platform API request that directly exercises this feature with a live bot token and record the response schema/error.
- **telegram / `keyboards.rubika.link`** — Not independently established by the Phase 1 audit. Probe: issue the smallest platform API request that directly exercises this feature with a live bot token and record the response schema/error.
- **telegram / `keyboards.rubika.ask_my_phone_number`** — Not independently established by the Phase 1 audit. Probe: issue the smallest platform API request that directly exercises this feature with a live bot token and record the response schema/error.
- **telegram / `keyboards.rubika.ask_my_location`** — Not independently established by the Phase 1 audit. Probe: issue the smallest platform API request that directly exercises this feature with a live bot token and record the response schema/error.
- **telegram / `keyboards.rubika.barcode`** — Not independently established by the Phase 1 audit. Probe: issue the smallest platform API request that directly exercises this feature with a live bot token and record the response schema/error.
- **telegram / `files.multi_step_upload`** — Not independently established by the Phase 1 audit. Probe: issue the smallest platform API request that directly exercises this feature with a live bot token and record the response schema/error.
- **bale / `messaging.sticker`** — Not independently established by the Phase 1 audit. Probe: issue the smallest platform API request that directly exercises this feature with a live bot token and record the response schema/error.
- **bale / `messaging.edit_media`** — Not independently established by the Phase 1 audit. Probe: issue the smallest platform API request that directly exercises this feature with a live bot token and record the response schema/error.
- **bale / `messaging.bulk_delete`** — Not independently established by the Phase 1 audit. Probe: issue the smallest platform API request that directly exercises this feature with a live bot token and record the response schema/error.
- **bale / `messaging.reactions`** — No confirmed Bale reaction contract in the audited project. Probe: issue the smallest platform API request that directly exercises this feature with a live bot token and record the response schema/error.
- **bale / `messaging.link_preview_control`** — Bale parse/formatting and link-preview behavior is not confirmed. Probe: issue the smallest platform API request that directly exercises this feature with a live bot token and record the response schema/error.
- **bale / `messaging.protect_content`** — Not established by the audited Bale method signatures. Probe: issue the smallest platform API request that directly exercises this feature with a live bot token and record the response schema/error.
- **bale / `keyboards.force_reply`** — No audited Bale ForceReply model. Probe: issue the smallest platform API request that directly exercises this feature with a live bot token and record the response schema/error.
- **bale / `keyboards.rubika.selection`** — Not independently established by the Phase 1 audit. Probe: issue the smallest platform API request that directly exercises this feature with a live bot token and record the response schema/error.
- **bale / `keyboards.rubika.calendar`** — Not independently established by the Phase 1 audit. Probe: issue the smallest platform API request that directly exercises this feature with a live bot token and record the response schema/error.
- **bale / `keyboards.rubika.number_picker`** — Not independently established by the Phase 1 audit. Probe: issue the smallest platform API request that directly exercises this feature with a live bot token and record the response schema/error.
- **bale / `keyboards.rubika.string_picker`** — Not independently established by the Phase 1 audit. Probe: issue the smallest platform API request that directly exercises this feature with a live bot token and record the response schema/error.
- **bale / `keyboards.rubika.location`** — Not independently established by the Phase 1 audit. Probe: issue the smallest platform API request that directly exercises this feature with a live bot token and record the response schema/error.
- **bale / `keyboards.rubika.camera_image`** — Not independently established by the Phase 1 audit. Probe: issue the smallest platform API request that directly exercises this feature with a live bot token and record the response schema/error.
- **bale / `keyboards.rubika.camera_video`** — Not independently established by the Phase 1 audit. Probe: issue the smallest platform API request that directly exercises this feature with a live bot token and record the response schema/error.
- **bale / `keyboards.rubika.gallery_image`** — Not independently established by the Phase 1 audit. Probe: issue the smallest platform API request that directly exercises this feature with a live bot token and record the response schema/error.
- **bale / `keyboards.rubika.gallery_video`** — Not independently established by the Phase 1 audit. Probe: issue the smallest platform API request that directly exercises this feature with a live bot token and record the response schema/error.
- **bale / `keyboards.rubika.file`** — Not independently established by the Phase 1 audit. Probe: issue the smallest platform API request that directly exercises this feature with a live bot token and record the response schema/error.
- **bale / `keyboards.rubika.audio`** — Not independently established by the Phase 1 audit. Probe: issue the smallest platform API request that directly exercises this feature with a live bot token and record the response schema/error.
- **bale / `keyboards.rubika.record_audio`** — Not independently established by the Phase 1 audit. Probe: issue the smallest platform API request that directly exercises this feature with a live bot token and record the response schema/error.
- **bale / `keyboards.rubika.textbox`** — Not independently established by the Phase 1 audit. Probe: issue the smallest platform API request that directly exercises this feature with a live bot token and record the response schema/error.
- **bale / `keyboards.rubika.link`** — Not independently established by the Phase 1 audit. Probe: issue the smallest platform API request that directly exercises this feature with a live bot token and record the response schema/error.
- **bale / `keyboards.rubika.ask_my_phone_number`** — Not independently established by the Phase 1 audit. Probe: issue the smallest platform API request that directly exercises this feature with a live bot token and record the response schema/error.
- **bale / `keyboards.rubika.ask_my_location`** — Not independently established by the Phase 1 audit. Probe: issue the smallest platform API request that directly exercises this feature with a live bot token and record the response schema/error.
- **bale / `keyboards.rubika.barcode`** — Not independently established by the Phase 1 audit. Probe: issue the smallest platform API request that directly exercises this feature with a live bot token and record the response schema/error.
- **bale / `chat_admin.restrict`** — Not independently established by the Phase 1 audit. Probe: issue the smallest platform API request that directly exercises this feature with a live bot token and record the response schema/error.
- **bale / `chat_admin.permissions`** — Not independently established by the Phase 1 audit. Probe: issue the smallest platform API request that directly exercises this feature with a live bot token and record the response schema/error.
- **bale / `chat_admin.join_requests`** — Not independently established by the Phase 1 audit. Probe: issue the smallest platform API request that directly exercises this feature with a live bot token and record the response schema/error.
- **bale / `chat_admin.forum_topics`** — Not independently established by the Phase 1 audit. Probe: issue the smallest platform API request that directly exercises this feature with a live bot token and record the response schema/error.
- **bale / `chat_admin.member_status_updates`** — Not independently established by the Phase 1 audit. Probe: issue the smallest platform API request that directly exercises this feature with a live bot token and record the response schema/error.
- **bale / `files.multi_step_upload`** — Not independently established by the Phase 1 audit. Probe: issue the smallest platform API request that directly exercises this feature with a live bot token and record the response schema/error.
- **bale / `bot_profile.commands`** — Not independently established by the Phase 1 audit. Probe: issue the smallest platform API request that directly exercises this feature with a live bot token and record the response schema/error.
- **bale / `bot_profile.name`** — Not independently established by the Phase 1 audit. Probe: issue the smallest platform API request that directly exercises this feature with a live bot token and record the response schema/error.
- **bale / `bot_profile.description`** — Not independently established by the Phase 1 audit. Probe: issue the smallest platform API request that directly exercises this feature with a live bot token and record the response schema/error.
- **bale / `inline_mode`** — Not independently established by the Phase 1 audit. Probe: issue the smallest platform API request that directly exercises this feature with a live bot token and record the response schema/error.
- **rubika / `messaging.audio`** — Not independently established by the Phase 1 audit. Probe: issue the smallest platform API request that directly exercises this feature with a live bot token and record the response schema/error.
- **rubika / `messaging.animation`** — Not independently established by the Phase 1 audit. Probe: issue the smallest platform API request that directly exercises this feature with a live bot token and record the response schema/error.
- **rubika / `messaging.sticker`** — Not independently established by the Phase 1 audit. Probe: issue the smallest platform API request that directly exercises this feature with a live bot token and record the response schema/error.
- **rubika / `messaging.venue`** — Not independently established by the Phase 1 audit. Probe: issue the smallest platform API request that directly exercises this feature with a live bot token and record the response schema/error.
- **rubika / `messaging.dice`** — Not independently established by the Phase 1 audit. Probe: issue the smallest platform API request that directly exercises this feature with a live bot token and record the response schema/error.
- **rubika / `messaging.copy`** — Not independently established by the Phase 1 audit. Probe: issue the smallest platform API request that directly exercises this feature with a live bot token and record the response schema/error.
- **rubika / `messaging.edit_caption`** — Not independently established by the Phase 1 audit. Probe: issue the smallest platform API request that directly exercises this feature with a live bot token and record the response schema/error.
- **rubika / `messaging.edit_media`** — Not independently established by the Phase 1 audit. Probe: issue the smallest platform API request that directly exercises this feature with a live bot token and record the response schema/error.
- **rubika / `messaging.bulk_delete`** — Not independently established by the Phase 1 audit. Probe: issue the smallest platform API request that directly exercises this feature with a live bot token and record the response schema/error.
- **rubika / `messaging.reactions`** — Not independently established by the Phase 1 audit. Probe: issue the smallest platform API request that directly exercises this feature with a live bot token and record the response schema/error.
- **rubika / `messaging.link_preview_control`** — Not independently established by the Phase 1 audit. Probe: issue the smallest platform API request that directly exercises this feature with a live bot token and record the response schema/error.
- **rubika / `messaging.protect_content`** — Not independently established by the Phase 1 audit. Probe: issue the smallest platform API request that directly exercises this feature with a live bot token and record the response schema/error.
- **rubika / `keyboards.force_reply`** — Not independently established by the Phase 1 audit. Probe: issue the smallest platform API request that directly exercises this feature with a live bot token and record the response schema/error.
- **rubika / `keyboards.button.url`** — Not independently established by the Phase 1 audit. Probe: issue the smallest platform API request that directly exercises this feature with a live bot token and record the response schema/error.
- **rubika / `keyboards.button.callback`** — Not independently established by the Phase 1 audit. Probe: issue the smallest platform API request that directly exercises this feature with a live bot token and record the response schema/error.
- **rubika / `keyboards.button.web_app`** — Not independently established by the Phase 1 audit. Probe: issue the smallest platform API request that directly exercises this feature with a live bot token and record the response schema/error.
- **rubika / `keyboards.button.copy_text`** — Not independently established by the Phase 1 audit. Probe: issue the smallest platform API request that directly exercises this feature with a live bot token and record the response schema/error.
- **rubika / `keyboards.button.color_style`** — Not independently established by the Phase 1 audit. Probe: issue the smallest platform API request that directly exercises this feature with a live bot token and record the response schema/error.
- **rubika / `keyboards.button.icon_emoji`** — Not independently established by the Phase 1 audit. Probe: issue the smallest platform API request that directly exercises this feature with a live bot token and record the response schema/error.
- **rubika / `keyboards.button.request_contact`** — Not independently established by the Phase 1 audit. Probe: issue the smallest platform API request that directly exercises this feature with a live bot token and record the response schema/error.
- **rubika / `keyboards.button.request_location`** — Not independently established by the Phase 1 audit. Probe: issue the smallest platform API request that directly exercises this feature with a live bot token and record the response schema/error.
- **rubika / `keyboards.button.request_poll`** — Not independently established by the Phase 1 audit. Probe: issue the smallest platform API request that directly exercises this feature with a live bot token and record the response schema/error.
- **rubika / `keyboards.button.request_users`** — Not independently established by the Phase 1 audit. Probe: issue the smallest platform API request that directly exercises this feature with a live bot token and record the response schema/error.
- **rubika / `keyboards.button.request_chat`** — Not independently established by the Phase 1 audit. Probe: issue the smallest platform API request that directly exercises this feature with a live bot token and record the response schema/error.
- **rubika / `keyboards.button.pay`** — Not independently established by the Phase 1 audit. Probe: issue the smallest platform API request that directly exercises this feature with a live bot token and record the response schema/error.
- **rubika / `keyboards.button.switch_inline`** — Not independently established by the Phase 1 audit. Probe: issue the smallest platform API request that directly exercises this feature with a live bot token and record the response schema/error.
- **rubika / `keyboards.button.login_url`** — Not independently established by the Phase 1 audit. Probe: issue the smallest platform API request that directly exercises this feature with a live bot token and record the response schema/error.
- **rubika / `callbacks.alert_toast`** — Not independently established by the Phase 1 audit. Probe: issue the smallest platform API request that directly exercises this feature with a live bot token and record the response schema/error.
- **rubika / `callbacks.data_byte_limit`** — button_id is confirmed but no callback-data byte limit is established. Probe: issue the smallest platform API request that directly exercises this feature with a live bot token and record the response schema/error.
- **rubika / `updates.webhook`** — Endpoint method is implemented; updateBotEndpoints parameter semantics remain UNKNOWN per R6. Probe: issue the smallest platform API request that directly exercises this feature with a live bot token and record the response schema/error.
- **rubika / `updates.allowed_updates`** — Endpoint/update parameters remain a documented UNKNOWN area. Probe: issue the smallest platform API request that directly exercises this feature with a live bot token and record the response schema/error.
- **rubika / `updates.long_poll_timeout`** — The audited method exposes no timeout parameter. Probe: issue the smallest platform API request that directly exercises this feature with a live bot token and record the response schema/error.
- **rubika / `chat_admin.restrict`** — Not independently established by the Phase 1 audit. Probe: issue the smallest platform API request that directly exercises this feature with a live bot token and record the response schema/error.
- **rubika / `chat_admin.promote`** — Not independently established by the Phase 1 audit. Probe: issue the smallest platform API request that directly exercises this feature with a live bot token and record the response schema/error.
- **rubika / `chat_admin.pin`** — Not independently established by the Phase 1 audit. Probe: issue the smallest platform API request that directly exercises this feature with a live bot token and record the response schema/error.
- **rubika / `chat_admin.unpin`** — Not independently established by the Phase 1 audit. Probe: issue the smallest platform API request that directly exercises this feature with a live bot token and record the response schema/error.
- **rubika / `chat_admin.invite_links`** — Not independently established by the Phase 1 audit. Probe: issue the smallest platform API request that directly exercises this feature with a live bot token and record the response schema/error.
- **rubika / `chat_admin.member_count`** — Not independently established by the Phase 1 audit. Probe: issue the smallest platform API request that directly exercises this feature with a live bot token and record the response schema/error.
- **rubika / `chat_admin.administrators`** — Not independently established by the Phase 1 audit. Probe: issue the smallest platform API request that directly exercises this feature with a live bot token and record the response schema/error.
- **rubika / `chat_admin.permissions`** — Not independently established by the Phase 1 audit. Probe: issue the smallest platform API request that directly exercises this feature with a live bot token and record the response schema/error.
- **rubika / `chat_admin.title`** — Not independently established by the Phase 1 audit. Probe: issue the smallest platform API request that directly exercises this feature with a live bot token and record the response schema/error.
- **rubika / `chat_admin.description`** — Not independently established by the Phase 1 audit. Probe: issue the smallest platform API request that directly exercises this feature with a live bot token and record the response schema/error.
- **rubika / `chat_admin.photo`** — Not independently established by the Phase 1 audit. Probe: issue the smallest platform API request that directly exercises this feature with a live bot token and record the response schema/error.
- **rubika / `chat_admin.join_requests`** — Not independently established by the Phase 1 audit. Probe: issue the smallest platform API request that directly exercises this feature with a live bot token and record the response schema/error.
- **rubika / `chat_admin.forum_topics`** — Not independently established by the Phase 1 audit. Probe: issue the smallest platform API request that directly exercises this feature with a live bot token and record the response schema/error.
- **rubika / `chat_admin.member_status_updates`** — Not independently established by the Phase 1 audit. Probe: issue the smallest platform API request that directly exercises this feature with a live bot token and record the response schema/error.
- **rubika / `payments`** — Not independently established by the Phase 1 audit. Probe: issue the smallest platform API request that directly exercises this feature with a live bot token and record the response schema/error.
- **rubika / `bot_profile.name`** — Not independently established by the Phase 1 audit. Probe: issue the smallest platform API request that directly exercises this feature with a live bot token and record the response schema/error.
- **rubika / `bot_profile.description`** — Not independently established by the Phase 1 audit. Probe: issue the smallest platform API request that directly exercises this feature with a live bot token and record the response schema/error.
- **rubika / `inline_mode`** — Not independently established by the Phase 1 audit. Probe: issue the smallest platform API request that directly exercises this feature with a live bot token and record the response schema/error.

## Phase 9 webhook live probes

- Rubika `GetSelectionItem` and `SearchSelectionItems` endpoint types are not registered by Peyk. Live-probe their request/response bodies before adding support.
- Rubika webhook payloads are parsed only from repository-confirmed `Update` and `InlineMessage` models. A live probe should record any additional webhook event shapes before expanding the parser.
- Telegram self-signed certificate upload is intentionally not exposed because the audited `set_webhook` client signature has no `certificate` parameter.
